[package]
name = "quantumfuse_blockchain"
version = "0.1.0"
edition = "2018"

[dependencies]
tokio = { version = "1.10.0", features = ["full", "macros"] }
serde = { version = "1.0.140", features = ["derive"] } # Downgraded version
serde_json = "1.0.67" # Downgraded version
sha2 = "0.9.8" # Downgraded version
hex = "0.4.2" # Downgraded version
rand = "0.8.3" # Downgraded version
rocket = { version = "0.4.10", features = ["json"] } # Downgraded version
rocket_okapi = { version = "0.4.1" } # Downgraded version
rocket_cors = { version = "0.5.2" } # Downgraded version
juniper = "0.15.9" # Downgraded version
juniper_rocket = "0.8.2" # Downgraded version
jsonwebtoken = "8.1.1" # Downgraded version
prometheus = "0.11.0" # Downgraded version
redis = "0.21.0" # Downgraded version
env_logger = "0.9.0" # Downgraded version
anstream = "0.5.2" # Downgraded version
tracing = "0.1.25" # Downgraded version
dotenv = "0.14.1" # Downgraded version
libp2p = { version = "0.40.0", features = ["tcp-tokio", "dns-tokio"] } # Downgraded version
substrate-api-client = "0.9.0" # Downgraded version
assert_cmd = "1.0.8" # Downgraded version
pem = "2.0.0" # Downgraded version

[dev-dependencies]
cargo-edit = "0.9.0" # Downgraded version
